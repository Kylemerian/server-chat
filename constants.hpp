#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

enum PAGES{
    LOGPAGE = 0,
    AUTHPAGE,
    MAINPAGE
};

enum WINDOWSIZE{
    HEIGHT = 1024,
    WIDTH = 768
};

enum BGCOLOR{
    RED = 44,
    GREEN = 44,
    BLUE = 44
};

#endif